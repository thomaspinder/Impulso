# Scenario conditions

The condition vocabulary for scenario analysis: `ShockPath` sets a
structural shock's path (historical counterfactuals, prescribed scenario
shocks), `VariablePath` pins a future endogenous path (conditional
forecasts, structural scenarios).

```{eval-rst}
.. currentmodule:: impulso.scenario

.. autosummary::
   :toctree: generated/
   :nosignatures:

   ShockPath
   VariablePath
```
